spring:
  main:
    allow-bean-definition-overriding: true

eureka:
  instance:
    preferIpAddress: true
    instance-id: ${spring.application.name}:${HOSTNAME}:${server.port}
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://service-registry:8070/eureka/

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info
    enabled-by-default: true

